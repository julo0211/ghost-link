{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacité par défaut de ghost link (fenêtre principale).",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}

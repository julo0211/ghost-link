{
  "name": "ghost-link-ui",
  "private": true,
  "version": "0.29.0",
  "description": "Frontend TypeScript de ghost link (compilé vers ui/js).",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}

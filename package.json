{
  "name": "ghost-link-ui",
  "private": true,
  "version": "0.25.2",
  "description": "Frontend TypeScript de ghost link (compilé vers ui/js).",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
